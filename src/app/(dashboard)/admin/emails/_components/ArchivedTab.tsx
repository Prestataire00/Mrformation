"use client";

/**
 * Scaffold em-c-1 — ArchivedTab (Tab Archivés).
 *
 * Implémentation complète en em-c-4 :
 *   - Liste des templates `is_active = false` en opacity-60
 *   - Boutons [Restaurer] (=is_active=TRUE) + [Supprimer définitivement]
 *   - Modal de confirmation forte (input texte "supprimer" requis)
 *   - Block delete si template référencé par formation_automation_rules
 *     ou crm_automation_rules.config.template_id
 *
 * En attendant, ce composant renvoie null.
 */
export function ArchivedTab() {
  return null;
}
