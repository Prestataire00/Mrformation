"use client";

/**
 * Scaffold em-c-1 — AutomationsTab (Tab Automatisations).
 *
 * Implémentation complète en em-c-5 — 3 sous-tabs distincts :
 *   - Relances (intégration reminder_settings + sélecteurs template_id)
 *   - Déclencheurs formation (formation_automation_rules)
 *   - Automatisations CRM (crm_automation_rules post fix RLS em-a-4)
 *
 * Liens profonds bidirectionnels vers Modèles (édition template lié).
 * En attendant, ce composant renvoie null.
 */
export function AutomationsTab() {
  return null;
}
