"use client";

/**
 * Scaffold em-c-1 — HistoryTab (Tab Historique).
 *
 * Implémentation complète en em-c-6 :
 *   - Filtres status/date/recipient/template chips
 *   - Click ligne → HistoryDetailSheet (shadcn Sheet slide-in droite)
 *   - Bouton "Renvoyer manuellement" dans le detail panel
 *   - Capture body envoyé (rendu HTML escaped) pour diagnostic Loris
 *
 * En attendant, ce composant renvoie null.
 */
export function HistoryTab() {
  return null;
}
